services:
  ryu-controller:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hw3-ryu-controller
    restart: unless-stopped
    ports:
      - "6653:6653"   # OpenFlow controller port
      - "8080:8080"   # REST API port
    volumes:
      - ./hw3_controller_ryu.py:/app/hw3_controller_ryu.py:ro